base_model: TinyLlama/TinyLlama-1.1B-Chat-v1.0
epochs: 5
batch_size: 2
gradient_accumulation_steps: 4
learning_rate: 3.0e-4
max_length: 512
warmup_steps: 10
save_steps: 50
lora_rank: 8
lora_alpha: 16
lora_dropout: 0.1
target_modules:
  - q_proj
  - k_proj
  - v_proj
  - o_proj
fp16: true
# Transformers 5.x needs torch>=2.6 to resume optimizer state.
fresh_start: true
